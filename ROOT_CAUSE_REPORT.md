# StockAnalysis 0.4.1 根因报告

日期：2026-08-30（Asia/Shanghai）

## 结论

旧工作簿中 A/H 两市六个重点字段均为 0/100，并非同一种“网站无数据”。其中同时存在字段解析缺失、批次异常传播、占位适配器、失效端点重复请求和进度计权错误。0.4.1 已修复能够在当前公开来源中可靠修复的部分；最终 Top100 仍有三列 0% 覆盖，因此发布状态保持阻断。

## 八项根因与处理

1. **A/H 发行时总市值整列空白**：旧实现只在取得 `post_issue_total_shares` 时计算，但东方财富解析器只尝试了未实际返回的少数字段名；字段组又会因上市日期等任一字段存在而整体标记为成功，掩盖发行后总股本缺失。现已改为字段级状态，加入东方财富历史股本结构解析；A 股再以同花顺上市日历次股本变动补全。
2. **市值增长率同步整列空白**：它依赖 `最新总市值 / 发行时总市值 - 1`。发行时总市值为空时，派生值必须为空。修复基础字段后自动恢复，未使用当前总股本、发行股数或募资额近似。
3. **港股失败丢掉 A 股大宗交易**：旧 `BlockTrades_Fetch()` 先取得 A 股批量结果，再在同一操作中逐只请求港股；港股抛异常后整个函数退出，A 股结果没有返回。现已按市场隔离并即时保存 A 股结果，单只港股失败也不终止其他港股。
4. **AASTOCKS 两个适配器是占位实现**：旧代码只访问 Block Trades/Money Flow 页面，随后固定返回 `None/MISSING`，没有解析日期、数值或单位。现已实现当日 Block Trades 快照和最近 5 个交易日资金流解析；由于它们不能证明完整年度或 22 日区间，不会冒充相应年度/22 日字段。
5. **A 股资金流约 600 次失败仍全空**：历史主域名在当前网络反复断连，旧逻辑仍按每家公司、多网络路径和重试组合重复访问；延迟端点只有单日记录，又没有真正的 22 日备源。现改为每市场最多 3 家运行内健康探测，任一探测未取得完整 22 日即整批切换；A 股使用同花顺每日资金面备源。Top100 中 5 日覆盖 100%，22 日覆盖 99%，历史主域失败请求降为 18 次传输尝试而非数百次逐股重试。
6. **进度条前快后慢**：旧计划把数千只完整证券池中的每只股票都计为行情工作单元，但行情实际是少量批量请求；真正逐公司的财务、发行和资金流权重反而过小。现准备阶段保持不确定进度、不占固定百分比；证券池确定后按阶段耗时基线和本次公司数累计，跨市场、批次和阶段不重置。
7. **此前没有 macOS 产物**：PyInstaller 不能在 Windows 上交叉生成可信 `.app`，且没有真实 Mac 构建记录或已执行的 macOS CI。0.4.1 提供严格的 `macos-14` workflow，但当前目录不是 Git 仓库，也没有可触发的远端提交，所以仍不能生成或伪造 `.app`。
8. **源码包缺少 packaging/tests/docs/dist**：旧交付方式只裁剪了源码目录，没有按构建脚本依赖建立完整工程清单，也没有用解压后自检约束交付包。当前工作区已具备源码、测试、spec、脚本、文档和 workflow；因真实 macOS 产物仍缺失，严格的完整工程 ZIP 尚未生成。

## 基线与当前结果

| 市场/字段 | 基线 | 0.4.1 Top100 | 状态 |
|---|---:|---:|---|
| A 股发行时总市值 / 市值增长率 | 0% / 0% | 100% / 100% | 已解除 |
| A 股大宗交易笔数 / 金额 | 0% / 0% | 100% / 100% | 已解除；84 家非零、16 家确认零 |
| A 股 5 日 / 22 日资金流 | 0% / 0% | 100% / 99% | 已解除整列空白 |
| 港股发行时总市值 / 市值增长率 | 0% / 0% | 81% / 81% | 达到 v3 目标阈值 |
| 港股年度大宗交易笔数 / 金额 | 0% / 0% | 0% / 0% | **发布阻断** |
| 港股 5 日 / 22 日资金流 | 0% / 0% | 100% / 0% | 22 日仍为**发布阻断** |

完整统计见 `artifacts/COVERAGE_BASELINE.md` 与 `artifacts/COVERAGE_FINAL.md`。
