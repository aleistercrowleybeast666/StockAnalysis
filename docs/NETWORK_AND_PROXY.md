# 网络、代理与重试（0.4.1）

- 默认网络模式为“国内数据源优先直连”：国内及 HKEX 来源先直连，传输失败后尝试系统代理；境外来源遵循系统代理环境。
- “全部直连”和“系统代理”仍保留在内部配置中用于排障，普通 GUI 不暴露端点、代理、并发或 Token。
- 每个域名共享限速器，使用连接池、明确超时和有限退避重试。
- 不存在跨运行负缓存或跨公司运行级熔断。历史资金流使用本次运行内 3 样本健康探测，避免对 Top100 重复请求同一失效端点。
- A 股历史主源不健康时切换同花顺；港股切换 TradeGo/AASTOCKS 的 5 日来源。严格 22 日仍缺少可验证来源时保持空白。
- 日志记录域名、功能端点、直连/代理、耗时、重试和回退原因，不记录访问令牌。
