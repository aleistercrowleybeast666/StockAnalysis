# 旧版构建归档

此目录保留从项目根目录 `dist` 移出的旧构建，避免误启动 0.3.x 或早期临时 0.4.0 EXE。

- 本目录根部的 `StockAnalysis.exe`、`_internal`、`logs` 原为旧 `dist/StockAnalysis`。
- `StockAnalysis_0.3.1_backup_before_0.3.2` 是更早的备份。
- `windows` 是采用旧目录名生成的临时 0.4.0 构建。

正式发布物只使用项目根目录 `dist/win`。这些归档未被删除，如需历史日志仍可在此查找。
